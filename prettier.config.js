/** @type {import("prettier").Config} */
const config = {
  plugins: ['prettier-plugin-tailwindcss'],
  tabWidth: 2,
  trailingComma: 'es5',
  singleQuote: true,
  printWidth: 120,
};

module.exports = config;
